const namespaces = 'accounts';

const CREATE_ACCOUNT = `${namespaces}/CREATE_ACCOUNT`;

const GET_ACCOUNT_INFO = `${namespaces}/GET_ACCOUNT_INFO`;

const GET_ALL_ACCOUNT = `${namespaces}/GET_ALL_ACCOUNT`;

const UPDATE_ACCOUNT = `${namespaces}/UPDATE_ACCOUNT`;

const CREDIT = `${namespaces}/CREDIT`;

const DEBIT = `${namespaces}/DEBIT`;

const TRANSFER = `${namespaces}/TRANSFER`;

export default {
	CREATE_ACCOUNT,
	GET_ACCOUNT_INFO,
	GET_ALL_ACCOUNT,
	UPDATE_ACCOUNT,
	CREDIT,
	DEBIT,
	TRANSFER
};
